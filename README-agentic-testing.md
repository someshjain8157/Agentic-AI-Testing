# Agentic testing suites

This folder contains the pytest-based agentic testing suites for the TeacherAI evaluation workflow:
- deepeval
- ragas
- pyrit
- playwright

Run the suites together with:

```powershell
pytest -q tests/suites
```

Allure output is written to reports/allure.
