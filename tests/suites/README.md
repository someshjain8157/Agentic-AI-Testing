# Test suites

This folder contains separate pytest suites for each evaluation stack:
- deepeval
- ragas
- pyrit
- playwright

Run them with:

```powershell
pytest -q tests/suites
```

Allure output is written to reports/allure.
